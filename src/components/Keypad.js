import React from "react";

import Button from "./UI/buttons/Button";

const Keypad = (props) => {
  return (
    <>
      <h2>Keypad</h2>
      <Button></Button>
    </>
  );
};

export default Keypad;
